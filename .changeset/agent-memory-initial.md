---
'agent-memory': minor
---

Add the Agent Memory MCP server: persistent, semantically searchable memory for agents backed by the authenticated user's own R2 bucket and Workers AI. Includes file read/write with tag and wikilink indexing, semantic search, conversation indexing, reminders, and an opt-out on-demand agentic reflection pass with a configurable generic notification webhook.

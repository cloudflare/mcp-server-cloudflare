---
'agent-memory': minor
---

Add the Agent Memory MCP server: persistent, semantically searchable, account-scoped memory backed by the selected Cloudflare account's R2 and Workers AI. Includes file read/write with tag and wikilink indexing, semantic search, conversation indexing, reminders, and an opt-out on-demand reflection pass that stages substantive edits for review and supports a generic notification webhook.
